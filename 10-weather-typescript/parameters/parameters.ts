import { Parameter } from './parameter.class.js';
import { TOKEN_KEY, CITY_KEY } from '../const.js';

const token = new Parameter(TOKEN_KEY);
const city = new Parameter(CITY_KEY);

export { token, city };
